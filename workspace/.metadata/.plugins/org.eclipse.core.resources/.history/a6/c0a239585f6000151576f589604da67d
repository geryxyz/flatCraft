package flatcraft.worlds;

import java.awt.Point;

import flatcraft.Sheep;
import flatcraft.blocks.Block;
import flatcraft.blocks.DirtBlock;
import flatcraft.blocks.DoorBlock;
import flatcraft.blocks.GrassBlock;
import flatcraft.blocks.TreeBlock;

public class SheepWorld extends FlatWorld {

	public SheepWorld(int rows, int columns) {
		super(rows, columns);
		spawnPosition = new Point(1, 6);
		
		Block dirt = new DirtBlock();
		Block grass = new GrassBlock();
		Block door = new DoorBlock();
		Block tree = new TreeBlock();
		
		// door
		placeBlock(19, 3, door);
		
		// valley
		breakBlock(9, 2);
		breakBlock(10, 2);
		breakBlock(11, 2);
		breakBlock(12, 2);
		breakBlock(13, 2);
		breakBlock(10, 1);
		breakBlock(11, 1);
		breakBlock(12, 1);
		
		// hill
		placeBlock(0, 5, grass);
		placeBlock(0, 4, dirt);
		placeBlock(0, 3, dirt);
		placeBlock(0, 2, dirt);
		
		placeBlock(1, 5, grass);
		placeBlock(1, 4, dirt);
		placeBlock(1, 3, dirt);
		placeBlock(1, 2, dirt);
		
		placeBlock(2, 5, grass);
		placeBlock(2, 4, dirt);
		placeBlock(2, 3, dirt);
		placeBlock(2, 2, dirt);
		
		placeBlock(3, 5, grass);
		placeBlock(3, 4, dirt);
		placeBlock(3, 3, dirt);
		placeBlock(3, 2, dirt);
		
		placeBlock(4, 4, grass);
		placeBlock(4, 3, dirt);
		placeBlock(4, 2, dirt);
		
		placeBlock(5, 3, grass);
		placeBlock(5, 2, dirt);
		
		// trees
		placeBlock(3, 6, tree);
		placeBlock(8, 3, tree);
		placeBlock(17, 3, tree);
		
	}

	@Override
	protected void onGameStart() {
		super.onGameStart();
		Sheep sheep = new Sheep();
		addPlayer(sheep);
		sheep.startPatrol();
	}

}
