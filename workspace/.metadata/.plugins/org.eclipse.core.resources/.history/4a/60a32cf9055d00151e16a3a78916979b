import flatcraft.Building;
import flatcraft.World;


public class Stairway extends Building {

	public Stairway(World world) {
		super(world);
	}

	@Override
	public void build() {
		for(int y = 3; y <= 7; y++) {
			for(int x = 13; x <= 17; x++) {
				world.placeBlock(x, y, stoneBlock);
			}
		}
	}
	
}
